//
//  SceneDelegate.swift
//  DoraPresentationTimer
//

import UIKit

/// メイン画面のシーン。Info.plist の `UIWindowSceneSessionRoleApplication`（Default Configuration）から生成される。
///
/// ウィンドウと中身は SwiftUI の `WindowGroup`（DoraPresentationTimerApp）が用意するので、ここでは何も作らない。
/// 外部ディスプレイ側の構成と対称にするためだけに存在する。
final class SceneDelegate: UIResponder, UIWindowSceneDelegate {
}
